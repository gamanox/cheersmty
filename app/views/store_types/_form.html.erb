<%= form_for(@store_type) do |f| %>
  <% if @store_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_type.errors.count, "error") %> prohibited this store_type from being saved:</h2>

      <ul>
      <% @store_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
